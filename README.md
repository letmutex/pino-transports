A sample of writing file, HTTP, and email transports for [pino.js](https://github.com/pinojs/pino).

```bash
# Setup env vars
cp .env.example .env
vi .env

# Install dependencies
pnpm install

# Run
node src/index.mjs
```
